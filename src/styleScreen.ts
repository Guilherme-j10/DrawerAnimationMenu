import { StyleSheet } from 'react-native';

export const StyleScreen = StyleSheet.create({
  MainContainer: {
    width: '100%',
    backgroundColor: '#fff',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center'
  }
});